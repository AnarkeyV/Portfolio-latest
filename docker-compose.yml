services:
  web:
    build: .
    ports:
      - "5001:5000"
    environment:
      - FLASK_ENV=production
    env_file:
      - .env
    volumes:
      - ./instance:/app/instance
      - ./data:/app/data
    restart: unless-stopped